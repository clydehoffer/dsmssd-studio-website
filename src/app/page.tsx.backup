'use client';

import { useEffect } from 'react';

export default function Home() {
  useEffect(() => {
    // Immediate redirect to landing-v2
    window.location.replace('/landing-v2');
  }, []);

  return null; // No content since we're redirecting immediately
} 